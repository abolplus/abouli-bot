ABOULI BOT - FINAL

Render Build Command:
pip install -r requirements.txt

Render Start Command:
python bot.py

Environment Variable:
BOT_TOKEN = BotFather token

IMPORTANT:
1. Add the bot as an administrator of @abolsniper. Membership checking depends on this.
2. Replace the old bot.py with this bot.py. Do not upload the old bot.py alongside it and accidentally run the old file.
3. Keep BOT_TOKEN only in Render Environment Variables.
4. The bot includes a small HTTP health endpoint for Render on 0.0.0.0:$PORT.
